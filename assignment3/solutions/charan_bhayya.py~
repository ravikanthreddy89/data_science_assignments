import re;
import json;

replaced_list={};
s="10011011001";

ip={"0110":"1001", "1001":"0","10":"11"};

for fi in ip.keys():
	match=re.search(fi, s);
	if(match):
		if(replaced_list.haskey(ip .get(fi))):
			continue;
		else:
			s.replace(find_and_replace_strings.get(fi),s,1);	
		
